## What this changes

<!-- What was wrong, and why this fixes it. The diff already says which files
     you touched, so spend the space on the reason. -->

## How it was tested

<!-- The part that matters. Which tests you added or ran, and against which
     SQL Server. "CI is green" is fine if the change is covered by a test you
     added; say so. -->

- [ ] `dotnet test tests/SqlDebugger.Sidecar.Tests` with `SQLDBGR_TEST_CONNECTION` set
- [ ] `npm test` in `extension/`
- [ ] Tried it by hand in the Extension Development Host

## Notes for the reviewer

<!-- Anything you are unsure about, decisions you made that could have gone the
     other way, or follow-up work you deliberately left out. Optional. -->
