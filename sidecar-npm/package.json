{
  "name": "tsql-debugger-sidecar",
  "version": "0.1.0",
  "description": "Sidecar service for the tsql-debugger VS Code extension. Runnable via npx; requires the .NET 8 runtime.",
  "license": "MIT",
  "bin": {
    "tsql-debugger-sidecar": "bin/tsql-debugger-sidecar.js"
  },
  "files": [
    "bin",
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "prepack": "npm run build"
  }
}
